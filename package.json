{
  "name": "@andrioid/andri-dk-root",
  "version": "1.1.0",
  "author": "Andri Oskarsson <andri@andri.dk>",
  "private": true,
  "keywords": [
    "andri",
    "portfolio"
  ],
  "license": "MIT",
  "scripts": {
    "build:site": "pnpm install -C packages/site && cd packages/site && pnpm run build",
    "build:react-cv": "pnpm install -C packages/react-cv && pnpm run -C packages/react-cv build",
    "build": "pnpm run build:react-cv && pnpm run build:site"
  },
  "devDependencies": {
    "prettier-plugin-astro": "^0.5.0",
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": "16.x",
    "pnpm": ">=7.9.5"
  },
  "cacheDirectories": [
    ".cache"
  ],
  "dependencies": {
    "prettier": "^2.6.2"
  }
}
